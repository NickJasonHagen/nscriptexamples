## basic file systems

```swift
filewrite("./filepath.txt","hello this is the file content")
filedata = fileread("./filepath.txt")
size = filesizebytes("./filepath.txt")
filecopy(file,copiedfile)
filemove(filelocation,movehere)
filedelete(file)
````
